import SwiftUI

struct RootView: View {
    var body: some View {
        TopView()
    }
}

struct RootView_Previews: PreviewProvider {
    static var previews: some View {
        RootView()
    }
}
